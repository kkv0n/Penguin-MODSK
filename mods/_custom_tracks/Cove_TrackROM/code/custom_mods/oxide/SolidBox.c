#include <common.h>
#include "../../header/cove_rom.h"


//since this function is not byte matching on decomp I CANT use that one
void _CTR_Box_DrawWireBox(RECT* rect, Color* rgb, struct OTMem* otMem, struct PrimMem* primMem)
{

  FUN_80021894(rect, rgb, otMem, primMem);
  
  return;
  
}